package Toppings;
/*import java.util.Scanner;

public class ToppingsDisplayed {
    public void meatDisplayed(Scanner scanner) {
        try {

            System.out.println("What type of meat would you like?");
            String meatType = scanner.nextLine();

            System.out.println("What size sandwich did you get?");
            System.out.println("4, 8 or 12");
            int inputSandwichSize = scanner.nextInt();
            scanner.nextLine();
            double price = 1.00;

            Meats meats = new Meats(meatType,price, inputSandwichSize);



            System.out.println("You have added a " + meats.getName());
            System.out.println("Price: $ " + String.format("%.2f",meats.getPrice()));
            System.out.println("Meat type: " + meats.getType());
            System.out.println("Meat size: " + meats.getSize());
            //System.out.println("The price of a " + getSize() + "\" sandwich is $" + String.format("%.2f", getPrice()));
        } catch (Exception e) {
            throw new RuntimeException(e);
        }

    }
}
*/